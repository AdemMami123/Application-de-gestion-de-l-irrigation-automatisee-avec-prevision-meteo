apiVersion: apps/v1
kind: Deployment
metadata:
  name: arrosage-service
  namespace: irrigation-system
  labels:
    app: arrosage-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: arrosage-service
  template:
    metadata:
      labels:
        app: arrosage-service
    spec:
      containers:
      - name: arrosage-service
        image: irrigation/arrosage-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8082
          name: http
        envFrom:
        - configMapRef:
            name: arrosage-service-config
        env:
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: postgres-arrosage-secret
              key: postgres-username
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: postgres-arrosage-secret
              key: postgres-password
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 120
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 5
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8082
          initialDelaySeconds: 90
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      initContainers:
      - name: wait-for-eureka
        image: busybox:1.35
        command: ['sh', '-c', 'until nc -z eureka-server 8761; do echo waiting for eureka-server; sleep 5; done;']
      - name: wait-for-postgres
        image: busybox:1.35
        command: ['sh', '-c', 'until nc -z postgres-arrosage 5432; do echo waiting for postgres-arrosage; sleep 2; done;']
      - name: wait-for-kafka
        image: busybox:1.35
        command: ['sh', '-c', 'until nc -z kafka 9092; do echo waiting for kafka; sleep 2; done;']
      - name: wait-for-redis
        image: busybox:1.35
        command: ['sh', '-c', 'until nc -z redis 6379; do echo waiting for redis; sleep 2; done;']
      - name: wait-for-meteo-service
        image: busybox:1.35
        command: ['sh', '-c', 'until nc -z meteo-service 8081; do echo waiting for meteo-service; sleep 5; done;']
