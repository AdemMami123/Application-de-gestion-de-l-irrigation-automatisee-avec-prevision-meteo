apiVersion: v1
kind: Service
metadata:
  name: postgres-meteo
  namespace: irrigation-system
  labels:
    app: postgres-meteo
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: postgres-meteo
