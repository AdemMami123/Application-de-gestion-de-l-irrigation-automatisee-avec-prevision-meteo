apiVersion: v1
kind: Service
metadata:
  name: postgres-auth
  namespace: irrigation-system
  labels:
    app: postgres-auth
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    name: postgres
  selector:
    app: postgres-auth
